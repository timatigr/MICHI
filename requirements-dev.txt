# Инструменты разработки (не нужны для запуска приложения).
# Установка: .venv\Scripts\pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3
httpx==0.28.1        # fastapi.testclient (API-тесты ходят через TestClient)
lameenc==1.8.4       # scripts/build_tts.py: пережатие WAV VOICEVOX в mp3 (без ffmpeg)
